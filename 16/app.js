// Import the arithmetic module
const arithmetic = require('./arithmetic');

// Use the functions from the arithmetic module
const sum = arithmetic.add(5, 3);
console.log('Sum:', sum);

const difference = arithmetic.subtract(10, 4);
console.log('Difference:', difference);

const product = arithmetic.multiply(2, 6);
console.log('Product:', product);

const quotient = arithmetic.divide(15, 3);
console.log('Quotient:', quotient);